# Core dependencies
aiohttp>=3.8.0
asyncio
numpy>=1.21.0

# Audio processing
pydub>=0.25.1
scipy>=1.7.0
soundfile>=0.11.0

# Whisper STT (optional - for local STT)
# openai-whisper>=20230314

# Web and API
requests>=2.28.0
websockets>=10.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
twilio>=9.0.0

# Utilities
python-dotenv>=0.19.0
pyyaml>=6.0

# Optional ML/audio
# Note: Install a matching `torch` for your platform (CUDA/no-CUDA). Example below is CPU-only wheel placeholder.
torch>=2.0.0
transformers>=4.40.0
datasets>=2.0.0
kokoro>=0.9.2